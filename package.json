{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "rm -fr out && cp -r src out && gleam compile-package --name my_app --target javascript --src out --out out && node out/index.js"
  },
  "dependencies": {
    "colors": "^1.4.0"
  }
}
